# Security policy

Please do not report security vulnerabilities in public issues. Send a private report to the repository maintainer with a description, reproduction steps, affected versions, and impact.

Do not include credentials, customer data, or production URLs in reports.

The project never relies on a user-agent string as an authentication or authorization signal; AI bot detection is advisory only.
